version: "3.8"
services:
    web:
        build:
            context: ./backend
        volumes:
            - ./backend:/app
        ports:
            - "8000:8000"

    frontend:
        build:
            context: ./frontend
        ports:
            - "3000:3000"
